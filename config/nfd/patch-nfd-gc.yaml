apiVersion: apps/v1
kind: Deployment
metadata:
  name: nfd-gc
  namespace: node-feature-discovery
spec:
  template:
    spec:
      tolerations:
        - key: gpu
          operator: Equal
          value: "true"
          effect: NoSchedule
